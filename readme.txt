Run local (with sync):
python dev-server.py

Local URL:
http://localhost:8000

To sync phone and desktop:
1. Login with the same account on both devices.
2. Open the same server from both devices, for example:
   http://<LAN-IP-of-your-computer>:8000
